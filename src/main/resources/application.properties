spring.application.name=ReserveMate
server.port=8081


# Solace Broker Connection Properties
spring.jms.pub-sub-domain=true
solace.jms.host=tcp://localhost:55555
solace.jms.msgVpn=default
solace.jms.clientUsername=default
solace.jms.clientPassword=default

# Optionally, enable SSL if required (otherwise, keep it as is for non-SSL connection)
# solace.jms.ssl.enabled=false
solace.jms.apiProperties.Solace_JMS_DynamicDurables=true

# Example of picking up an environment/config property:
solace.jms.demoQueueName=tutorial/queue

# Solace logging example:
logging.level.com.solacesystems=INFO

# You can adjust the connection settings depending on your Solace configuration and requirements


# spring.cloud.stream.binders.solace.type=solace
# spring.cloud.stream.binders.solace.environment.solace.java.host=tcp://localhost:55555
# spring.cloud.stream.binders.solace.environment.solace.java.msgVpn=default
# spring.cloud.stream.binders.solace.environment.solace.java.clientUsername=admin
# spring.cloud.stream.binders.solace.environment.solace.java.clientPassword=admin
# spring.cloud.stream.solace.host=smf://localhost:55555
# spring.cloud.stream.solace.username=admin
# spring.cloud.stream.solace.password=admin
# spring.cloud.stream.solace.vpn=default
